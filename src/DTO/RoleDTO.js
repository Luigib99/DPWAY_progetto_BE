class RoleDTO {
    constructor({ id, name, description, createdDate }) {
        this.id = id;
        this.name = name;
        this.description = description;
        this.createdDate = createdDate;
    }
}

module.exports = RoleDTO;
